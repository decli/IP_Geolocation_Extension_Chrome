{
  "name": "ip-geolocation-extension-chrome",
  "version": "3.1.1",
  "private": true,
  "description": "Reliable IPv4, IPv6 and geolocation monitor for Chrome",
  "license": "GPL-3.0-only",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check background.js && node --check js/constants.js && node --check js/main.js && node --check js/models/GeoLocation.js && node --check js/options.js && node --check js/popup.js && node --check js/utils/LocalStorageProvider.js && node --check js/utils/RefreshPolicy.js",
    "build": "bash build.sh package chrome"
  },
  "engines": {
    "node": ">=20"
  }
}
